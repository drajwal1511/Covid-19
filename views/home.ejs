<%- include("partials/header") %> 
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
    <a class="navbar-brand" href="/">COVID 19 TRACKER</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>  
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link" href="/covid">Home <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Link</a>
        </li>
        <li class="nav-item">
          <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
        </li>
      </ul>
      <form method="POST" action="/covid" class="form-inline my-2 my-lg-0">
        <input name="country" class="form-control mr-sm-2" type="search" placeholder="Search a Country" aria-label="Search">
        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
      </form>
    </div>
  </div>
  </nav>
  <main>
    <section>
      <% if(isglobal){ %>
        <div class="jumbotron">
          <div class="container">
            <h2><img src="<%= img %>">
                WORLD
            </h2>
            <div class="row">
              <div class="confirmed col-lg-4 col-md-4 col-sm-12"><%= global.TotalConfirmed %>(+<%= global.NewConfirmed %>)<div><h4>Confirmed</h4></div> </div>
              <div class="recovered col-lg-4 col-md-4 col-sm-12"><%= global.TotalRecovered %>(+<%= global.NewRecovered %>)<div><h4>Recovered</h4></div></div>
              <div class="deaths col-lg-4 col-md-4 col-sm-12"><%= global.TotalDeaths %>(+<%= global.NewDeaths %>)<div><h4>Deaths</h4></div></div>
            </div>
          </div>
        </div>
        <div class="jumbotron">
            <table class="table table-striped">
              <thead class="thead-dark">
                <th>Country</th>
                <th>Confirmed</th>
                <th>Recovered</th>
                <th>Deaths</th>
              </thead>
              <tbody>
                <% for(var i=0;i<countries.length;i++){ %> 
                  <tr>
                    <td>
                      <form action="/covid" method="POST">
                        <button name="country" value="<%= countries[i].Country %>" type="submit"><img src="<%= countries[i].imgflag %>"> <%= countries[i].Country %></button>  
                      </form>
                    </td>
                    <td>
                      <span class="confirmed">
                        <%= countries[i].TotalConfirmed %>
                      </span>
                      +<%= countries[i].NewConfirmed %> 
                    </td>
                    <td>
                      <span class="recovered">
                        <%= countries[i].TotalRecovered %>
                      </span>
                      +<%= countries[i].NewRecovered %> 
                    </td>
                    <td>
                      <span class="deaths"> 
                        <%= countries[i].TotalDeaths %>
                      </span>
                      +<%= countries[i].NewDeaths %> 
                    </td>
                  </tr>
                <% } %> 
              </tbody>
            </table>
        </div>
      <% }else{ %> 
        <div class="jumbotron">
          <div class="container">
            <h2><img src="<%= country.imgflag %>">
                <%= country.Country %> 
            </h2>
            <div class="row">
              <div class="confirmed col-lg-4 col-md-4 col-sm-12"><%= country.TotalConfirmed %>(+<%= country.NewConfirmed %>)<div><h4>Confirmed</h4></div> </div>
              <div class="recovered col-lg-4 col-md-4 col-sm-12"><%= country.TotalRecovered %>(+<%= country.NewRecovered %>)<div><h4>Recovered</h4></div></div>
              <div class="deaths col-lg-4 col-md-4 col-sm-12"><%= country.TotalDeaths %>(+<%= country.NewDeaths %>)<div><h4>Deaths</h4></div></div>
            </div>
          </div>
        </div>
      <% } %> 

    </section>
  </main>
<%- include("partials/footer") %> 